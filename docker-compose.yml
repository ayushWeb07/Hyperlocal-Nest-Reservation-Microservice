services:
  mongo:
    image: mongo
    container_name: hyperlocal-nest-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: